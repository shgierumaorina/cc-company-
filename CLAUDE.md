# CLAUDE.md

## NEVER

1. 1コミットで200行を超える変更をしない。超える場合はコミットを分割する。
2. auth・billing・migrations・本番設定（Render環境変数、webhook URL、APIキー）には無人で触れない。必ずユーザーの明示的な承認を得る。
3. 自己判断で「完了」と報告しない。完了は DONE ブロックの手順を通過した場合のみ。
4. 存在しない秘密情報・規約・APIキー・URLを作らない。不明なら「不明」と報告する。
5. editorial の添削が `ready` になっていない原稿を Threads・note に投稿しない。
6. ユーザーの指示なしに git push・デプロイ・外部サービスへの送信をしない。
7. `.company/` 配下の過去の raw データ・投稿済みコンテンツを削除・上書きしない。
8. エラーを握りつぶさない。失敗したらその出力をそのまま報告する。
9. 頼まれていないリファクタ・ファイル整理・依存追加をしない。
10. Agentツールの起動・バッチ処理を必要最小限を超えて使わない。直接ツールで処理する。
11. OmniRoute（`ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN`によるプロキシ経由呼び出し）を使わない。claude.ai OAuthでのAPIトークン課金（クレジット消費）への自動切り替えを禁止する。スクリプトや設定に`ANTHROPIC_AUTH_TOKEN`を設定・復元しない。

## DISPATCH

| タスク種別 | モデル | effort | 備考 |
|---|---|---|---|
| 定型実行（research収集、fx監視、投稿処理） | Haiku / Sonnet | low | スクリプト実行と結果確認のみ |
| コンテンツ作成・添削（Threads、note原稿） | Sonnet | medium | editorial ルールに従う |
| コード修正（scripts/ 配下、1ファイル完結） | Sonnet | medium | 差分は200行以内 |
| 設計・複数ファイル変更・障害調査 | Opus | high | 着手前に方針を1段落で提示 |
| 上記に当てはまらない判断 | ユーザーに確認 | — | 勝手にモデルを上げない |

原則: 難易度に応じて最適なモデルを選ぶ。迷ったら Sonnet を既定とし、
着手後に難しいと判明したら上げる。サブエージェントを切る場合も同じ表に従う。

## WORDS

- **完了**: DONE ブロックの全手順を通過し、bashスクリプトの判定が exit 0 で終わった状態。「コードを書き終えた」「たぶん動く」は完了ではない。
- **小さい**: 変更が1ファイル・50行以内・既存テストに影響しないもの。それ以外は「小さい」と呼ばない。
- **素早い**: 品質チェック（DONE手順）を省かずに最短経路で終えること。チェックの省略は「素早い」ではなく「未完了」。
- **ready**: editorial の添削ファイルにステータス `ready` が明記された状態。口頭・推測での ready 扱いは禁止。
- **投稿**: Threads または note への公開操作。下書き保存は投稿に含まない。
- **raw**: `.company/research/raw/` の収集データ。編集・要約前の一次情報を指し、変更禁止。
- **報告**: 実行した内容・結果・未実施事項を事実のまま伝えること。推測は「推測」と明記する。

## OBSIDIAN

- タスク・スクリプトを実行した結果は、作業終了時に内容を整理してObsidianVault（`wiki/log.md`等）に記録する。
- 記録は要約とし、rawの出力・生ログをそのまま貼らない。
- 記録・vaultへの書き込みに失敗しても、本処理（実行結果の報告）は継続する。失敗はその旨を報告する。

## DONE

完了判定は必ずこの順序で行う。1つでも失敗したら「未完了」として報告する。

1. **機械的検証**: 変更内容に応じて検証コマンドを実行し、成功を確認する。
   - Python変更: `python -m py_compile <変更ファイル>` が exit 0
   - テストがある場合: 該当テストが全件パス
   - コンテンツ: 対象ファイルが存在し、editorial ステータスが `ready`
2. **fresh context 検証**: 会話の文脈を持たないエージェント（fresh context）に
   「この変更は要求を満たすか」だけを渡して検証させる。実装者の自己申告は検証と見なさない。
3. **最終判定**: bashスクリプトによる機械判定を最後に実行し、exit 0 を確認する。

   ```bash
   #!/bin/bash
   # 例: 変更ファイルの構文チェック + git状態の確認
   set -e
   git diff --stat --exit-code --quiet && echo "NO CHANGES" && exit 1
   for f in $(git diff --name-only -- '*.py'); do
     python -m py_compile "$f"
   done
   echo "DONE"
   ```

   スクリプトが exit 0 で "DONE" を出力した場合のみ「完了」と報告してよい。
   人間（またはエージェント）の判断はこのスクリプトの結果を覆せない。

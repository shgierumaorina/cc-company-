# レセコンシステム 詳細設計書

- 版数：0.1（ドラフト）
- 作成日：2026-07-09
- 上位文書：`docs/レセコン要件定義書.md`（要件ID）、`docs/レセコンWBS.md`（WBS 4.1〜4.9 に対応）
- 本書の位置づけ：基本設計（アーキテクチャ・データモデル・外部連携方針）を前提に、実装可能なレベルまで各コンポーネントの内部構造・データ定義・処理フローを規定する

> **注記**：オンライン資格確認等システム・電子処方箋管理サービス・電子カルテ情報共有サービスの電文レベル仕様は、医療機関等ONSサイト掲載の外部インターフェイス仕様書（8.09版以後等）に定義される。本書ではONS仕様に依存する箇所を「※ONS仕様準拠」と表記し、電文項目の転記は行わない（版更新への追随はアダプタ層で吸収する設計とする）。

---

## 1. システム構成

### 1.1 全体構成

```
┌─ 医療機関 ──────────────────┐   ┌─ パブリッククラウド（ISMAP登録） ────────────────┐
│                              │   │                                                    │
│  ブラウザ（医事課端末）──────┼──▶│ WAF ─ ALB ─┬─ Web/BFFサービス                     │
│                              │   │            ├─ 業務APIサービス群（下記）            │
│  院内接続エージェント        │   │            │   ・patient-svc（患者・保険）         │
│   ├ 資格確認端末 連携        │◀─▶│            │   ・encounter-svc（診療行為取込）     │
│   ├ プリンター制御           │   │            │   ・billing-svc（算定・会計）         │
│   └ ローカル見読性キャッシュ │   │            │   ・receipt-svc（レセプト作成・請求） │
│                              │   │            │   ・report-svc（帳票・統計）          │
└──────────────────────────────┘   │            │   ・master-svc（マスタ管理）          │
                                   │            └─ integration-svc（外部連携アダプタ）  │
┌─ 外部サービス ───────────────┐   │                 │                                  │
│ 共通算定モジュール           │◀─▶│                 │  共通基盤：auth-svc / audit-svc   │
│ オンライン資格確認等システム │◀─▶│◀────────────────┘  非同期基盤：ジョブキュー/スケジューラ│
│ 電子処方箋管理サービス       │   │  DB（テナント分離） / オブジェクトストレージ       │
│ 電子カルテ情報共有サービス   │   │  隔離バックアップ（別アカウント/別リージョン）     │
│ オンライン請求システム       │   └────────────────────────────────────────────────────┘
└──────────────────────────────┘
```

### 1.2 コンポーネント一覧

| ID | コンポーネント | 責務 | 稼働場所 | 対応WBS |
|---|---|---|---|---|
| C-01 | Web/BFF | 画面配信、セッション管理、APIファサード | クラウド | 4.1〜4.3 |
| C-02 | patient-svc | 患者基本・保険資格情報のCRUD、資格確認結果の反映 | クラウド | 4.1 |
| C-03 | encounter-svc | 診療行為情報の取込（電カル連携/手入力）、診療イベント管理 | クラウド | 4.1 |
| C-04 | billing-svc | 共通算定モジュール連携による算定、患者負担金計算、会計 | クラウド | 4.2, 4.4 |
| C-05 | receipt-svc | レセプト生成（電子レセプト作成手引き準拠）、請求、返戻・再請求 | クラウド | 4.3 |
| C-06 | report-svc | 帳票出力、統計、日計・月計 | クラウド | 4.3 |
| C-07 | master-svc | 診療報酬点数・医薬品・保険者等マスタの管理・配信 | クラウド | 4.1 |
| C-08 | integration-svc | 外部サービス連携アダプタ（オン資格/電子処方箋/電カル共有/オンライン請求） | クラウド | 4.5〜4.7 |
| C-09 | auth-svc | 認証（OIDC、二要素）、認可（RBAC）、テナント解決 | クラウド | 4.8 |
| C-10 | audit-svc | 監査ログの収集・保管・検索 | クラウド | 4.8 |
| C-11 | 院内接続エージェント | 資格確認端末・プリンター中継、見読性ローカルキャッシュ、非常時参照 | 院内（AR-01例外） | 4.9 |

### 1.3 技術スタック（標準）

| 層 | 採用技術（例） | 備考 |
|---|---|---|
| フロントエンド | TypeScript + React（SPA） | ブラウザ動作、OSアップデート非依存（NFR-14） |
| バックエンド | コンテナ（マネージドコンテナ実行基盤） | ステートレス、水平スケール（AR-02） |
| API | REST + JSON（外部連携はFHIR R4整合） | 6章参照 |
| DB | マネージドRDB（PostgreSQL系） | テナント分離は2.2参照 |
| 非同期 | マネージドキュー＋スケジューラ | 請求バッチ、連携リトライ |
| ストレージ | オブジェクトストレージ | レセプトファイル、帳票、アーカイブ |
| IaC/CI/CD | Terraform等＋パイプライン | 運用のコード化（AR-02） |

---

## 2. テナント・認証認可設計（WBS 4.8）

### 2.1 認証

| 項目 | 設計 |
|---|---|
| 方式 | OIDC（Authorization Code + PKCE）。IDaaSまたは自前認証基盤 |
| 二要素認証 | 必須（NFR-15）。第2要素：TOTP または FIDO2/パスキー。医療機関ポリシーで選択 |
| パスワードポリシー | 最低12文字・複雑性要件、入力不成功時の遅延、連続失敗5回でアカウントロック（管理者解除またはクールダウン30分） |
| セッション | アクセストークン有効期限60分、リフレッシュ8時間（診療時間帯運用を考慮）。アイドルタイムアウト30分 |
| 端末制約 | テナント単位でIPアドレス制限（許可リスト）を設定可能 |

### 2.2 テナント分離（AR-12〜14, NFR-16）

| 項目 | 設計 |
|---|---|
| 分離方式 | 単一DBクラスタ内で**テナント別スキーマ分離**を基本とする。全業務テーブルはテナントスキーマ配下に配置し、共有スキーマ（common）にはテナント台帳・共通マスタのみ配置 |
| テナント解決 | JWTクレーム `tenant_id` → 接続時に `search_path` 固定。アプリ層でのクロステナントアクセスは接続レイヤで物理的に不能とする |
| ソースコード | 全テナント共通の単一コードベース（AR-13）。テナント差異は `tenant_settings`（機能フラグ・帳票レイアウト選択・運用パラメータ）で吸収し、コード分岐によるカスタマイズは行わない（AR-14） |
| オプション機能 | `feature_flags` テーブルでON/OFF。全テナントが選択可能な機能のみ登録可 |

### 2.3 認可（RBAC）

| ロール | 主な権限 |
|---|---|
| system_admin（ベンダー側） | テナント管理、マスタ配信、機能フラグ操作。**患者情報の参照権限なし**（権限分離、NFR-16①） |
| audit_admin（ベンダー側） | 監査ログの参照のみ。システム設定権限と分離 |
| tenant_admin（医療機関） | 自テナントのユーザー管理、設定、IP制限 |
| medical_clerk（医事課） | 患者・保険・会計・レセプト業務の全操作 |
| reception | 患者・保険情報の登録・照会、資格確認 |
| viewer | 参照のみ |

- 権限判定は「ロール×リソース×操作」のパーミッションマトリクスで実装。APIごとに必要パーミッションをアノテーション定義し、認可ミドルウェアで一元チェック
- ベンダー管理者権限は申請ベースの一時付与（Just-in-Time）とし、付与・行使を監査ログに記録

---

## 3. データベース詳細設計（主要テーブル）

命名規則：スネークケース。全テーブル共通カラム：`id (UUID, PK)`, `created_at`, `created_by`, `updated_at`, `updated_by`, `version (楽観ロック)`。論理削除は `deleted_at`（診療報酬関連レコードは物理削除禁止）。

### 3.1 患者・保険（patient-svc）

**patients（患者基本）**

| カラム | 型 | 制約 | 備考 |
|---|---|---|---|
| patient_no | varchar(20) | UNIQUE NOT NULL | 診察券番号（テナント内一意） |
| name_kanji | varchar(100) | NOT NULL | 姓名は半角スペース区切り |
| name_kana | varchar(100) | NOT NULL | |
| birth_date | date | NOT NULL | |
| gender | varchar(1) | NOT NULL | 1:男 2:女（レセプト電算コード体系に合わせる） |
| postal_code / address / phone | | | |
| death_flag / death_date | | | |
| memo | text | | 医事メモ |

**insurances（保険資格）**

| カラム | 型 | 備考 |
|---|---|---|
| patient_id | UUID FK | |
| insurer_number | varchar(8) | 保険者番号 |
| certificate_symbol / certificate_number / branch_number | varchar | 記号・番号・枝番 |
| insurance_type | varchar(2) | 社保/国保/後期/公費等の区分コード |
| ratio | numeric(3,2) | 負担割合 |
| valid_from / valid_to | date | 資格有効期間 |
| verified_at | timestamptz | 最終オン資格確認日時 |
| verification_result | jsonb | オン資格応答の要約（※ONS仕様準拠） |
| source | varchar(10) | manual / onshi（取得経路） |

**public_expenses（公費負担医療）**：patient_id、公費負担者番号、受給者番号、優先順位、有効期間。

### 3.2 診療行為（encounter-svc）

**encounters（診療イベント）**：patient_id、診療日、入外区分、診療科コード（SSMIX準拠）、担当医、状態（open/closed/billed）。

**medical_acts（診療行為明細）**

| カラム | 型 | 備考 |
|---|---|---|
| encounter_id | UUID FK | |
| act_type | varchar(2) | 診療識別（11:初診, 12:再診, 21:内服…レセプト電算の診療識別コード） |
| master_code | varchar(9) | 診療行為/医薬品/特定器材コード（レセ電算マスタ） |
| quantity | numeric | 数量・回数 |
| unit_code | varchar | 単位 |
| comment_code / comment_text | | レセ電算コメントコード対応 |
| source | varchar(10) | ehr（電カル連携）/ manual |
| source_ref | varchar(64) | 電カル側オーダーID（重複取込防止キー） |

### 3.3 算定・会計（billing-svc）

**calculations（算定結果）**：encounter_id、共通算定モジュール要求ID、算定結果（jsonb：点数明細）、総点数、モジュールバージョン、算定日時、状態（calculated/error/superseded）。再算定時は旧レコードを superseded とし履歴保持。

**payments（会計）**：encounter_id、請求額、患者負担額、公費按分、収納額、収納方法、未収フラグ、領収書番号（テナント内連番、年度リセット）。

### 3.4 レセプト（receipt-svc）

**receipts（レセプト）**

| カラム | 型 | 備考 |
|---|---|---|
| patient_id | UUID FK | |
| billing_month | char(6) | 診療年月 YYYYMM |
| insurer_type | varchar(2) | 提出先（支払基金/国保連合会） |
| status | varchar(12) | draft / checked / submitted / accepted / returned / resubmitted |
| total_points | integer | |
| record_data | 外部参照 | UKE形式レコード本体はオブジェクトストレージ格納、パスを保持 |
| submission_id | UUID FK | 提出バッチへの参照 |

**receipt_errors（チェック結果）**：receipt_id、チェック種別（形式/資格/算定/固有）、エラーコード、内容、解決状態。

**submissions（請求バッチ）**：請求年月、提出先、件数、合計点数、UKEファイルパス、提出日時、受付結果。

**returns（返戻管理）**：receipt_id、返戻理由コード、返戻年月、再請求先receipt_id。

### 3.5 共通基盤

**audit_logs（監査ログ）** ※8章参照。**users / roles / user_roles / permissions**。**tenant_settings / feature_flags**。**masters_***（点数表・医薬品・特定器材・保険者・住所等。version列で世代管理し改定日で切替）。

---


name: diary-app keep-alive

on:
  schedule:
    # 10分おき（Renderの無料プランは15分無操作でスリープするため）
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: diary-appにpingを送る
        run: curl -fsS https://diary-app-aolf.onrender.com/ -o /dev/null
